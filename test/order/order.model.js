export class Order {
 constructor(id, orderNumber, numbers, clientId, productsIds) {
  this.id = id
  this.orderNumber = orderNumber
  this.numbers = numbers
  this.clientId = clientId
  this.productsIds = productsIds
 }
}